<Workflow>
	<Start/>

	<!--<UserTask name="suu"/>-->
<Script name="setStageInputParam" output="$.requestDataList.[0]" saveAs="jn" path="$.requestDataList">
	<parameter name="stage" type="constant" value="X_verify" />
		<ScriptText>
			<![CDATA[
import json

data = json.loads(context)
for requestData in data:
    if 'inputParam' in requestData:
        for param in requestData['inputParam']:
            if 'value' in param and param['value']:
                param['stage'] = stage  # Set or modify the 'stage' value
json.dumps(data)
			]]>
		</ScriptText>
	</Script>
 
	<Finish/>
</Workflow>

